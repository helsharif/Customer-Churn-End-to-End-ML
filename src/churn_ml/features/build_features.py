"""Placeholder feature engineering module for future project phases."""

import pandas as pd


def build_features(df: pd.DataFrame) -> pd.DataFrame:
    """Return features unchanged until project-specific transforms are added."""
    return df.copy()
